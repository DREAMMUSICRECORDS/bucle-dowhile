alert("Bienvenido a royalnieto");
var entradas = 30;
var contador = 0;
var estado = "s"

var entradas = parseInt (prompt ("escribe el numero de entradas"));
 if (entradas <=30 && entradas >= 1){
    alert ("entradas disponibles");
    


    }
    else if  (entradas2 >=31 && entradas >= 31 ){
     alert ("no hay suficientes entradas");
    }


  const numeroentradas = 30;
  const numeroqueseingresa = prompt("ingrasa otra vez el numero de entradas que quieres:");
  resultado = numeroentradas - numeroqueseingresa;
  Number.isNaN(numeroentradas - numeroqueseingresa);

  alert( resultado = numeroentradas-numeroqueseingresa);

  var entradas = parseInt (prompt ("escribe el numero de entradas"));